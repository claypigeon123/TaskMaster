<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.c.taskmaster">

    <application
        android:name=".notifications.NotificationWrapper"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activites.NewCharacterActivity"/>
        <activity android:name=".activites.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.SettingsActivity" />
        <activity android:name=".activites.StatsActivity" />
        <activity android:name=".activites.NewTaskActivity" />
        <activity android:name=".activites.TaskListActivity" />
        <activity android:name=".activites.MainActivity" />
    </application>

</manifest>